DBNStore methods

NextRecord: returns next record from the Data source

REcord methods
// Gets the record as a specific type (throws if wrong type)
const auto& msg = record->Get<TradeMsg>();

// Safely checks and casts (returns nullptr if wrong type)
if (const auto* msg = record->GetIf<TradeMsg>()) {
    // use msg
}


ts_event is "The matching-engine-received timestamp expressed as the number of nanoseconds since the UNIX epoch. See ts_event." 

record documentation build/_deps/databento-src/include/databento/record.hpp
